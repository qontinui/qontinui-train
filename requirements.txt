# Core ML frameworks
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
pillow>=10.0.0

# Vision transformers and architectures
timm>=0.9.0  # PyTorch Image Models - comprehensive model library
transformers>=4.35.0  # Hugging Face transformers (ViT, Swin, etc.)
einops>=0.7.0  # Tensor operations for transformers

# Detection transformers
# detr - install from source if needed
# mmcv>=2.0.0  # For MMDetection

# Distributed training
pytorch-lightning>=2.1.0
accelerate>=0.24.0  # Hugging Face distributed training
deepspeed>=0.12.0  # Microsoft DeepSpeed for large-scale training
fairscale>=0.4.13  # Facebook's training utilities

# Self-supervised learning
# lightly>=1.4.0  # Self-supervised learning library
# timm has MAE, MoCo implementations

# Data handling (large-scale)
webdataset>=0.2.0  # Efficient large dataset handling
# ffcv>=1.0.0  # Fast data loading (requires compilation)
opencv-python>=4.8.0
albumentations>=1.3.0
pycocotools>=2.0.7

# Multi-modal learning
# clip @ git+https://github.com/openai/CLIP.git  # OpenAI CLIP
# open-clip-torch>=2.20.0  # OpenCLIP for training from scratch

# Experiment tracking and monitoring
wandb>=0.16.0
tensorboard>=2.15.0
mlflow>=2.8.0

# Optimization and efficiency
nvidia-dali-cuda120>=1.30.0; platform_system=="Linux"  # GPU-accelerated data loading
triton>=2.1.0  # For custom CUDA kernels

# Dataset creation and synthetic generation
# unity-ml-agents>=0.30.0  # For Unity-based generation
selenium>=4.15.0  # For web UI scraping
beautifulsoup4>=4.12.0
requests>=2.31.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.1
matplotlib>=3.8.0
seaborn>=0.13.0
pandas>=2.1.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Model export and optimization
onnx>=1.15.0
onnxruntime-gpu>=1.16.0
# tensorrt>=8.6.0  # NVIDIA TensorRT (requires separate installation)

# Development and testing
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.11.0
flake8>=6.1.0
mypy>=1.7.0
jupyter>=1.0.0
ipywidgets>=8.1.0
jupyterlab>=4.0.0

# Documentation
sphinx>=7.2.0
sphinx-rtd-theme>=1.3.0
